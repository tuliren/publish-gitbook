name: 'Publish Gitbook'

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Publich Gitbook
      uses: tuliren/publish-gitbook@v0.5.0
      env:
        GITHUB_TOKEN: ${{ github.token }}
