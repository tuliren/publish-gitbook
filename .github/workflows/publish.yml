name: 'Publish Gitbook'

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Publich Gitbook
      uses: tuliren/publish-gitbook
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
